worker_processes 1;
events { worker_connections 1024; }
http {
    server {
        listen <%= ENV["PORT"] %>;
        root /app;
        location / {
            index index.html;
            try_files $uri $uri/ =404;
        }
    }
}
